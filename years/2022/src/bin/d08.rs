#![allow(warnings, unused)]

use lib::prelude::*;

#[entry(input = "d08.txt", expect = (0, 0))]
fn main(input: IStr) -> Result<(u64, u64)> {
    let mut part1 = 0;
    let mut part2 = 0;

    Ok((part1, part2))
}
